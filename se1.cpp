#include <iostream>
#include <vector>
#include <set>

class Driver {
private:
	int salary;
	int workhour;
	int wage;
public:
	Driver(int workhour, int wage) {this->workhour = workhour, this->wage = wage};
	int getSalary() {};
};

int main() {
  int N;
  std::cin >> N;
}
